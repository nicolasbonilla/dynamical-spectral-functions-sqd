# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# LaTeX build artifacts (keep main.pdf, ignore the rest)
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk
*.bbl
*.blg
paper/*.aux
paper/*.log
paper/figs/*.aux
paper/figs/*.log

# Scaling checkpoints (regenerated by scaling_lanczos_mf.py)
ckpt/

# Stale / backup data (NOT the plotted data — see docs/FIGURE_PROVENANCE.md)
*_backup.json
*_dense_backup.json

# OS
.DS_Store
Thumbs.db

# Secrets — never commit credentials
.ibm_credentials
*token*.txt

# CI workflow held back: gh token lacks the "workflow" scope.
# To enable CI: run  gh auth refresh -s workflow  then delete the two lines below and: git add .github/workflows/ci.yml && git commit -m "add CI" && git push
.github/workflows/ci.yml
